extends layout

block head
    link(rel="stylesheet", href="/stylesheets/search.css")

block content
    #results
        #count
            span= results.total
            &nbsp;
            span Items

        ul
            each hit in results.hits
                li
                    a(href= '/items/' + hit.id)= hit.name

    #detail
        &nbsp;